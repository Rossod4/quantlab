# caches & generated output (anchored to repo root: these are the runtime
# cache/report dirs from configs/platform.yaml, NOT src/quantlab/data/ etc.)
/data/
/reports/
.pytest_cache/
.ruff_cache/
__pycache__/
*.pyc
.venv/
.coverage
htmlcov/

# secrets — NEVER commit broker keys
.env
*.secret.*
