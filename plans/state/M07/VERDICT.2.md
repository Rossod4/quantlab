VERDICT: ACCEPT

Gate cycle 2 on M07 (reporting). All three cycle-1 blockers are closed and all eight
non-blocking items are addressed. Every closure below was re-derived against the
iteration-3 tree by re-running the cycle-1 reproductions and by building fresh hostile
and degenerate cases of my own — not by re-reading HANDOFF.3.md or REVIEW.3.md. The gate
mutated no source, test, config or fixture file (`git status --short` shows only the
developer's own four modified files plus this cycle's `QUANT-NOTES.md` update; the four
fixture checksums are identical before and after my checks).

Verification re-run:

| check | result |
|---|---|
| `uv run pytest tests/ -q` | exit 0, 735 dots, ~80 s |
| `uv run ruff check .` / `format --check .` | clean / 107 files formatted |
| external `http(s)` refs in all four HTML fixtures | 0 |
| largest rendered HTML | 348 KB (bar: 5 MB) |
| HTML entities left in the markdown twins | 0 |

`configs/validation.yaml` is not in the diff: production `b: 200`, `block_len: 6.0`,
`monte_carlo_n_paths: 500` are untouched, so the reduced suite-time figures come from the
test fixture's own deliberately small bootstrap and are not masking a production change.

---

## Blocker 1 — walk-forward comparison transpose: CLOSED, re-reproduced cell by cell

I re-ran cycle 1's exact reproduction (10 steps, 2 children, 5 grid points, seed 7) and
checked every rendered cell against `WalkForwardResult.comparison` **as a DataFrame**,
not against the JSON — so a matching bug in the serialiser could not hide inside a
matching bug in the consumer:

```
Fixed 0% momentum/100% value   rendered=['2.98%','4.93%','0.60','-11.23%']  OK
Fixed 100% momentum/0% value   rendered=['5.45%','9.43%','0.52','-17.15%']  OK
Fixed 25% momentum/75% value   rendered=['3.70%','4.38%','0.79','-6.63%']   OK
Fixed 50% momentum/50% value   rendered=['4.35%','5.32%','0.74','-5.90%']   OK
Fixed 75% momentum/25% value   rendered=['4.93%','7.18%','0.61','-9.42%']   OK
Walk-Forward                   rendered=['5.07%','5.85%','0.85','-11.23%']  OK
COMPARISON TABLE MATCHES DATAFRAME: True     n/a cells: 0
```

Six rows, six grid points, zero `n/a`, in both HTML and markdown.
`_transpose_comparison_by_grid_point` re-keys at the boundary, which is the right place —
it leaves `WalkForwardResult.to_json()` (a read-only interface under the packet) alone.
The test fixture is now generated by a real `walk_forward_blend(...).to_json()`, so the
consumer can no longer be certified against a shape the producer does not emit; that was
the actual root cause and it is properly fixed, not patched around.

## Blocker 2 — chosen-weight sequence and sensitivity surface PNG-only: CLOSED

Chosen weights: all 10 steps render as 10 distinct table rows in markdown and 13 in the
shipped fixture's HTML, each weight matching `chosen_weights` exactly
(`CHOSEN-WEIGHT TABLE MATCHES DATAFRAME: True`). The `trim_blocks` newline problem is real
and the precomputed-line fix is the correct shape of fix; I confirmed empirically that
rows land one per line rather than collapsing.

Sensitivity surface: now a real table in both formats with the base point labelled. I
built a case the shipped fixtures do not cover — a **2-D** grid (3x2) with a planted NaN
cell and `neighbourhood_truncated=True` — and all six points render, the base point is
marked, and the NaN cell is visible and annotated rather than blank:

```
| lookback_months=12, skip_months=3 (base point) | 2.00 |
| lookback_months=15, skip_months=3 | n/a (NaN (excluded)) (excluded from no_cliff_score) |
```

That answers the cycle-1 concern about a heatmap hiding the base point or NaN cells: the
reader now gets both from text, not only from a PNG. (One cosmetic blemish in that cell —
see residual 2.)

**Escaping survived the rewrite**, which was the thing most at risk: the new precomputed
markdown lines (`chosen_weight_header`, `markdown_line`) interpolate `child_labels`, which
are config-sourced. They are plain `str`, not `Markup`, so they escape — and they are
referenced only by `report.md.j2`, never by the HTML template, which builds its own `<tr>`
loops. The Markup allowlist grew by exactly one entry (`VERDICT_LEGEND`) and that entry is
a fixed literal. I re-ran the cycle-1 hostile-ticker injection through the real
`price_panel_missing_tickers` -> capacity-gate-reason route against the new tree:
escaped in HTML (`&lt;script&gt;alert(1)&lt;/script&gt;`), page still closes `</html>`,
raw in the markdown twin as intended.

## Blocker 3 — old-repo capacity reference range: CLOSED

`OLD_REPO_CAPACITY_RANGE_USD` / `_ASSUMPTION` are constants in `capacity.py`, not template
literals, and render beside this run's own ceiling: "For scale only, NOT the output of this
run: the old repo capacity estimate on real 2012-2026 S&P 500 data was
$95,000,000-$335,000,000 AUM, under an assumed 50-name book (1/50 position_frac)." The
framing is right — a reader cannot mistake the reference for this run's output, and the
assumption that generated it is stated.

## The eight non-blocking items: all closed

- **Unscored-names caveat** now branches on the run's own `data_semantics_version` rather
  than asserting one static meaning, and names the measured figures (173 dates, 467-476
  names against 30 holdings). This is better than what I asked for: the flag's meaning
  genuinely changed at the M04b boundary, and the caveat now tracks that instead of
  hard-coding the pre-M04b reading.
- **Infinity** renders "unbounded (n/a)" in the badge line and the gate Value column.
- **Section-6 coverage** closed by the fourth fixture, whose fully populated
  `backtest_config` makes `test_section_6_execution_mode_note_present` assert the real
  `next_open` sentence and assert the "not recorded" fallback is *absent* — never both,
  never neither. The behaviour was already right in cycle 1; now it is pinned.
- **Plot failures visible.** Verified by monkeypatching `plot_equity_curves` to raise:
  both formats render "plot unavailable: ValueError: synthetic boom" and the failure is
  logged. No longer indistinguishable from a legitimately absent plot.
- **`--config` seed passthrough**, **untrusted-fraction flag selected by content**,
  **verdict/gate legend**, and the **cosmetics** (deduped coverage sentence, plain
  separators, rolling truncation with "Showing first/last 5 of 49 rows", markdown-safety
  docstring) are all present and confirmed in the rendered fixtures.

The legend deserves a specific note: it states plainly that hard failures cap at REJECTED,
soft failures cap at RESEARCH_ONLY, the two informational notes never affect the verdict,
and "a verdict is not evidence of edge; it states which tests the result survived." That
was the highest-leverage missing sentence on the page and it is now the first thing under
the badge.

---

## Residual items (none blocking; carried to M09)

1. **`report_card.py`'s own reason prose still says "inf".** The
   `min_track_record_length` row now reads Value `unbounded (n/a)` beside a Reason saying
   "needs >= inf observations for significance". **I rule the developer's scoping correct.**
   `ReportCard` is a read-only input under the packet's "Interfaces to honor", M07 formats
   every number it owns, and `test_infinity_never_renders_bare` documents the boundary
   explicitly rather than quietly excluding the case. The row is internally inconsistent to
   look at but not misleading — both halves convey the same true fact (no sample size
   attains significance at a zero Sharpe). Fix it upstream in M09, not by having the
   reporting layer rewrite prose it does not own.
2. **Cosmetic, reproduced:** a NaN sensitivity cell renders
   `n/a (NaN (excluded)) (excluded from no_cliff_score)` — nested parentheses and the word
   "excluded" three times in one cell. The `_num` reason and the template's own suffix say
   the same thing twice. One-line fix whenever the file is next open.
3. **Still open upstream, still correctly disclosed:** per-step training Sharpes are not
   retained on `WalkForwardResult`, and the blend-of-net versus netted-book Sharpe RANKING
   is still unchecked. Both print honest "cannot be verified here" / "not checked" lines and
   neither fabricates a figure. These belong to whoever next touches
   `validation/walk_forward.py`.
4. **Suite wall time has grown 53 s -> ~80 s** against a 90 s budget as the fixture set
   reached four rendered verdicts. Not M07's problem to solve, but M08 is about to add to
   this suite and the headroom is thin.
5. **Fixture realism, worth knowing before anyone reads it as an example:** the
   `walk_forward` fixture's walk-forward covers 2008-2020 while its headline result covers
   2015-2019, because the walk-forward series is synthetic and bolted onto the headline
   rather than derived from it. A real `validate --full` run builds both from the same
   child results. The report does disclose the walk-forward's own step dates, so the window
   is visible to a reader; no product defect.

## Recommendation

ACCEPT. The report is now a genuine honesty surface: every carried caveat sits beside the
number it qualifies, the two tables that were empty or PNG-only carry real numbers in both
formats, the verdict badge states what it does and does not mean, a failed plot says so,
and hostile vendor or YAML strings cannot break the page. Cycle 1's positives (autoescape
design, plot semantics, execution-mode truthfulness, sections 3/4/6 carried-note coverage)
were re-checked where the rewrite could have disturbed them and are intact.
