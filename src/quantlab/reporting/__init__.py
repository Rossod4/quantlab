"""M07: turns a `BacktestResult` + `ReportCard` into a self-contained HTML
report and a markdown twin. See `context.py` (number formatting),
`plots.py` (matplotlib, Agg backend), and `render.py` (the public
`render_report` entry point and the templates)."""

from __future__ import annotations
